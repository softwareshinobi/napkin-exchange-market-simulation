create system user on boot, if empty

create mateo user on boot, if empty

set the system to track more significant digits

configure random number seeding

import links from worksace

import docs 

import build situation

